<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.teamRocket.pokemonandroidapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.PokemonAndroidApp"
        android:usesCleartextTraffic="true">
        <activity android:name=".teams.lstTeam.view.LstTeamsActivity"/>
        <activity android:name=".pokemon.lstPokemon.view.LstPokemonView" />
        <activity android:name=".trainer.login.view.LoginActivity" />
        <activity android:name=".pokemon.detailPokemon.view.DetailPokemonActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>